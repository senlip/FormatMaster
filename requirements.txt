# FormatMaster 运行依赖
# 安装：pip install -r requirements.txt

# ---- 界面 ----
PySide6>=6.6

# ---- 图像引擎 ----
Pillow>=10.0
pillow-heif>=0.15          # HEIC / HEIF（手机照片）支持

# ---- 文档引擎 ----
PyMuPDF>=1.24              # PDF 读写、PDF↔图片、文本排版
python-docx>=1.1           # DOCX 生成与解析
openpyxl>=3.1              # XLSX 读写
mammoth>=1.6               # DOCX → HTML / 纯文本
Markdown>=3.5              # Markdown → HTML

# ---- 压缩引擎 ----
py7zr>=0.21                # 7z / zip / tar 纯 Python 读写

# ---- Windows 文档高保真通道（调用本机 WPS / Office）----
pywin32>=306; sys_platform == "win32"
